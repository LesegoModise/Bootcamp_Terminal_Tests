// export function greet(name) { //Just like any function, I named my funtion 'greet' followed by a 'name' parameter.
//     return ('Hello, ' + name); //This is the result once the code passes.
//   }


export function greet(name) {
    return `Hello, ${name}`;
}
  
  console.log(greet('Bob'));
  console.log(greet('Sam'));//The function will return as 'Hello, Bob'